<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <%- include('../partials/head') %>
  
</head>
<body>
  <%- include('../partials/nav') %>

  <div class="container mb-3 ">
        <div class ="first" style="text-align: center; margin-bottom: 10px;">
          <h1 style ="font-weight: 700; font-size:35pt; color:rgba(255, 94, 0,0.9)">회원가입</h1>
          <hr style="height:1" >
          <small  class="form-text">저희 사이트를 이용하시려면 회원가입이 필요합니다.</small>
          <small class ="form-text">이미 회원 이신가요? <a href="/login">로그인</a></small>
        </div>
   
  <form action ='/users' method ='POST'>
      <fieldset>

        <div class = "information" style="width:300px; margin: 0 auto;">
          <div class="form-group">
              <input type="text" class="form-control <%= (errors.name)? 'is-invalid': '' %>" id="name" name="name" value ="<%= user.name %>"placeholder="이름">
              <% if(errors.name){ %> <!-- 3 -->
                <span class="invalid-feedback"><%= errors.name.message %></span>
              <% } %>
            </div>
            <div class="form-group">
                <input type="text" class="form-control  <%= (errors.email)?'is-invalid': '' %>" value="<%= user.email %>" id="email" name="email" aria-describedby="email" placeholder="이메일@example.com">
                <% if(errors.email){ %> <!-- 3 -->
                  <span class="invalid-feedback"><%= errors.email.message %></span>
                <% } %>
              </div>
              <div class="form-group row" style="margin-left:1px;">
                  <select class="custom-select col-sm-3" id="phone1" name="phone1">
                      <option selected>010</option>
                      <option>016</option>
                      <option>011</option>
                      </select>       
              <div class ="col-sm-9">
                  <input type="text" class="form-control <%= (errors.phone)?'is-invalid': '' %>" id="phone2" name="phone2" value="<%= user.phone %>" placeholder="전화번호">
                  <% if(errors.phone){ %> <!-- 3 -->
                    <span class="invalid-feedback"><%= errors.phone.message %></span>
                  <% } %>
              </div>
              <small id="phone" class="form-text text-muted" style="text-align:left; padding-top:3px;">ex) 010-12345678</small>
            </div>
          </div>
              <hr style="height:1" >


          <div class = "information" style="width:300px; margin: 0 auto;">
            <div class ="form-group">
              <input type="text" class="form-control  <%= (errors.username)?'is-invalid': '' %>" id="username" name="username" value="<%= user.username %>" placeholder="아이디">
              <% if(errors.username){ %> <!-- 3 -->
                <span class="invalid-feedback"><%= errors.username.message %></span>
              <% } %>
            </div>
            <div class="form-group">
              <input type="password" class="form-control  <%= (errors.password)?'is-invalid': '' %>" id="password" name="password" value="<%= user.password %>" placeholder="비밀번호">
              <% if(errors.password){ %> <!-- 3 -->
                <span class="invalid-feedback"><%= errors.password.message %></span>
              <% } %>
              <small id="phone" class="form-text text-muted" style="text-align:left; padding-top:3px;">비밀번호는 8자리 이상이어야 합니다.</small>
            </div>
            <div class="form-group">
              <input type="password" class="form-control <%= (errors.passwordConfirmation)?'is-invalid': '' %>" id="passwordConfirmation" value="<%= user.passwordConfirmation %>" name="passwordConfirmation" placeholder="비밀번호 확인">
              <% if(errors.passwordConfirmation){ %> <!-- 3 -->
                <span class="invalid-feedback"><%= errors.passwordConfirmation.message %></span>
              <% } %>
            </div>
          </div>
          <hr style="height:1">

          <div class = "information" style="width:300px; margin: 0 auto;"> 
            <p style="text-align: center;">
              <small>*필수항목</small>
                <small id="email" class="form-text text-muted">우리는 절대로 회원정보를 타인에게 알려주지 않습니다..</small>
            </p>
            <% if(errors.unhandled){ %> <!-- 4 -->
              <div class="alert alert-danger">
                <%= errors.unhandled %>
              </div>
            <% } %>    
                <div class="form-group" style="text-align:center;">
                  <button type="submit" class="btn btn-primary">확인</button>
                </div>
              </div>
              </form>
    </fieldset>
  </div>
  
</body>
</html>