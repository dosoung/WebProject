<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <%- include('../partials/head') %>
  
</head>
<body>
  <%- include('../partials/nav') %>
  <div class="container mb-3 ">
      <div class ="first" style="text-align: center; margin-bottom: 10px;">
        <h1 style =" padding-top: 50px; font-weight: 700; font-size:35pt; color:rgba(255, 94, 0,0.9)">로그인</h1>
        <hr style="height:1" >
        <small  class="form-text">저희 사이트를 이용하시려면 회원가입이 필요합니다.</small>
        <small class ="form-text">아직 회원이 아니신가요? <a href="/users/new">회원가입</a></small>
      </div>
  <div class="container mb-3 ">
   
  <form action ='/login' method ='POST'>
          <div class = "information" style="width:300px; margin: 0 auto;">
            <div class ="form-group">
              <input type="text" class="form-control  <%= (errors.username)?'is-invalid': '' %>" id="username" name="username" value="<%= username %>" placeholder="아이디">
              <% if(errors.username){ %> <!-- 3 -->
                <span class="invalid-feedback"><%= errors.username %></span>
              <% } %>
            </div>
            <div class="form-group">
              <input type="password" class="form-control  <%= (errors.password)?'is-invalid': '' %>" id="password" name="password" value="" placeholder="비밀번호">
              <% if(errors.password){ %> <!-- 3 -->
                <span class="invalid-feedback"><%= errors.password%></span>
              <% } %>
              <small id="phone" class="form-text text-muted" style="text-align:left; padding-top:3px;">비밀번호는 8자리 이상이어야 합니다.</small>
            </div>
          </div>
          <hr style="height:1">

          <div class = "information" style="width:300px; margin: 0 auto;"></div>
            <% if(errors.login){ %> <!-- 4 -->
              <div class="alert alert-danger">
                <%= errors.login %>
              </div>
            <% } %>    
                <div class="form-group" style="text-align:center;">
                  <button type="submit" class="btn btn-primary">확인</button>
                </div>
              </div>
              </form>
  </div>
  
</body>
</html>